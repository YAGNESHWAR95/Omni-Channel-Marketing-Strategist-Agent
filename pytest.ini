[pytest]
# This line explicitly adds the current directory (the project root) to the 
# Python path, ensuring 'main_agent' can be imported by the tests.
python_files = tests/test_*.py
python_paths = .
