name: base # or your desired environment name
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - anaconda
  - pip:
    - google-adk
    - google-genai
    - pydantic
    # Add any other packages from your requirements.txt
