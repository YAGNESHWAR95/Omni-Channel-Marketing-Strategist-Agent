name: base # or your desired environment name
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - anaconda
  - pip:
    - google-adk
    - google-genai
    - pydantic
    - pytest
    - asyncio
    - deprecated
    - pandas
    - tabulate
    - tqdm
    - scikit-learn
    - google-genai
    - pydantic
    - numpy
